/** Rejected titles stay off Discover for this long (matches `api/movies` + app-state). */
export const DISCOVER_REJECT_HIDE_WINDOW_MS = 7 * 24 * 60 * 60 * 1000;

/** Favorite genres cap (onboarding + profile) — focused signal for Discover matching. */
export const FAVORITE_GENRE_LIMIT = 5;
